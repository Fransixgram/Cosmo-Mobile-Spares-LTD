// src/pages/AdminCategories.tsx
//
// Placeholder — created so the dashboard's "Categories" nav item and
// Quick Action have somewhere real to point to. Category management
// itself is a later phase.

export default function AdminCategories() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Admin - Categories</h1>
    </div>
  );
}

// src/pages/AdminSettings.tsx
//
// Placeholder — created so the dashboard sidebar's "Settings" nav item
// has somewhere real to point to, rather than being a dead link. Settings
// functionality itself is a later phase.

export default function AdminSettings() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Admin - Settings</h1>
    </div>
  );
}
